apiVersion: v2
name: paperless-ngx
description: A Helm chart to install paperless-ngx

type: application

version: 0.1.0

appVersion: "2.1.2"

dependencies:
  - name: redis
    version: "18.5.0"
    repository: "https://charts.bitnami.com/bitnami"
